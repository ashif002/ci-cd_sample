name : CI-CD

on : push


jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: check out code
      uses: actions/checkout@v3

    - name: Run a test
      run: echo "done"

    - name: Error checking
      run: echo "failed to run."; exit 1
